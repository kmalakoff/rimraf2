{
  "name": "rimraf2",
  "version": "2.8.0",
  "main": "rimraf.js",
  "description": "A deep deletion module for node (like `rm -rf`)",
  "author": "Isaac Z. Schlueter <i@izs.me> (http://blog.izs.me/)",
  "license": "ISC",
  "repository": "https://github.com/kmalakoff/rimraf2.git",
  "scripts": {
    "preversion": "npm test",
    "postversion": "npm publish",
    "postpublish": "git push origin --all; git push origin --tags",
    "test": "tap test/*.js"
  },
  "bin": "./bin.js",
  "files": [
    "LICENSE",
    "README.md",
    "bin.js",
    "rimraf.js"
  ],
  "peerDependencies": {
    "glob": "*"
  },
  "peerDependenciesMeta": {
    "glob": {
      "optional": true
    }
  },
  "devDependencies": {
    "glob": "^7.1.3",
    "mkdirp": "^0.5.6",
    "tap": "^9.0.3"
  }
}
